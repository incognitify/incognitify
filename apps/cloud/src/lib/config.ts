/**
 * Public source repository (open-core monorepo). `packages/core` is Apache-2.0 — use
 * freely; the hosted `apps/cloud` is source-available (FSL) — readable and self-hostable,
 * but not re-hostable as a competing commercial service. Surfaced in the product (footer,
 * FAQ, playground) as a transparency / auditability signal.
 */
export const REPO_URL = 'https://github.com/incognitify/incognitify';
